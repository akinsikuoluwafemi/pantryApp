import React from 'react'

export default function Layout({children}) {
    return (
        <div c>
            Header component
            {children}
            Footer Component
        </div>
    )
}
