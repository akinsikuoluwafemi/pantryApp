import React from 'react'

export default function Layout({children}) {
    return (
        <div className="">
            Header component
            {children}
            Footer Component
        </div>
    )
}
